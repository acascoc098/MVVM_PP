<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:name=".HotelAplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/illustration_of_the_beer_mugs_toasting_free_vector"
        android:label="@string/app_name"
        android:roundIcon="@drawable/illustration_of_the_beer_mugs_toasting_free_vector"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoPersonalizado"
        tools:targetApi="31">

        <activity
            android:name=".objets_models.Registro"
            android:exported="false" />
        <activity
            android:name=".ui.view.MainActivity"
            android:exported="false" />
        <activity
            android:name=".objets_models.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>